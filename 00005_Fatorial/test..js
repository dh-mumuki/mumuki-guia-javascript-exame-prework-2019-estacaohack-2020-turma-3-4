describe("", () => {
  it("fatorial(1)", () => {
    assert.equal(fatorial(1), 1);
  })
  it("fatorial(2)", () => {
    assert.equal(fatorial(2), 2);
  })
  it("fatorial(3)", () => {
    assert.equal(fatorial(3), 6);
  })
  it("fatorial(4)", () => {
    assert.equal(fatorial(4), 24);
  })
  it("fatorial(5)", () => {
    assert.equal(fatorial(5), 120);
  })
  it("fatorial(6)", () => {
    assert.equal(fatorial(6), 720);
  })
  
})